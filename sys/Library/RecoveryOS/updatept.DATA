#!/bin/bash

function updatept() {
	if [[ -f "$ROOTFS/System/boot/partitions.hdp" ]]; then
		nano "$ROOTFS/System/boot/partitions.hdp"
	else
		colorprint "${RED}System is not installed!"
	fi
}

export -f updatept