#!/bin/bash

function postinstconfigure() {
	if [[ -d "$ROOTFS/System/__MACOSX" ]]; then
		echo "Cleaning things up..."
		rm -rf "$ROOTFS/System/__MACOSX"
	fi

	if [[ -d "$ROOTFS/recovery_dimg" ]]; then
		rm -rf "$ROOTFS/recovery_dimg"
	fi
}

export -f postinstconfigure