#!/bin/bash

if [[ -f "$CORE/resources/bootscreen" ]]; then
	cat "$CORE/resources/bootscreen"
fi
if [[ -f "$CORE/resources/coreversion" ]]; then
	echo -n "Starting Hermes Core - v."
	cat "$CORE/resources/coreversion"
else
	echo "[-] Warning: Core version not found."
fi
